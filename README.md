# Collector External API

Start with [AGENTS.md](AGENTS.md). This source-derived reference uses synthetic examples, not live records. Obtain the deployment URL and issued credentials separately from the operator.

Only reviewed documentation belongs here: no application source, credentials, internal configuration or customer/respondent data. Generated files are maintained by the application's offline generator and pinned for drift checks. Publication requires separate review; generation never commits or pushes.
